name: Website Deployment
run-name: 🚀 ${{ github.actor }} is creating a new deployment of the website 🚀
on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
jobs:
  Deploy-website:
    runs-on: ubuntu-latest
    
    strategy:
      matrix:
        node-version: [18.x]
    
    steps:
      - uses: actions/checkout@v3
      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v3
        with:
          node-version: ${{ matrix.node-version }}
          
      - run: echo "🎊 Starting Deployment of josband.github.io"
      - name: Deployment
        working-directory: ./client/
        run: npm run deploy
